import { AnimatedPanel } from "@/components/AnimatedPanel"; import { Footer } from "@/components/Footer"; import { SecondaryNav } from "@/components/SecondaryNav";
export default function Security(){return <main className="shell compact"><SecondaryNav/><AnimatedPanel><article className="card policy-card"><p className="eyebrow">Stay safe</p><h1>Security</h1><p>Never share a UPI PIN, OTP or bank password. Enter your PIN only in a trusted UPI app.</p><p>Verify the recipient name and amount shown by that app before every payment. Do not pay an unexpected session, a request with a wrong amount, or a recipient you do not recognise.</p></article></AnimatedPanel><Footer/></main>}
