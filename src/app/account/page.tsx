import Link from "next/link";
import { Footer } from "@/components/Footer";
export default function AccountPage(){return <main className="shell compact"><header className="site-header"><Link className="brand" href="/">mdr-ama<span>™</span></Link></header><section className="card account-card"><p className="eyebrow">Merchant account</p><h1>Your QR history, in one place.</h1><p className="lead">Accounts will let merchants save templates and review sessions they created—amount, payee, creation time and expiry. Payment settlement remains outside MDR-ama unless an authorised provider integration exists.</p><div className="account-actions"><button className="button" disabled>Continue with email — setup required</button><Link className="secondary" href="/dashboard">View dashboard preview</Link></div><p className="fineprint">To enable real sign-in, configure the Supabase URL and anonymous key in Vercel. No account data is collected until that connection is configured.</p></section><Footer/></main>}
