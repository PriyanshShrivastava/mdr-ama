import { Footer } from "@/components/Footer"; export default function Privacy(){return <main className="shell compact"><header><a className="brand" href="/">mdr-ama™</a></header><article className="card"><h1>Privacy</h1><p>MDR-ama is a client-only MVP. Payment-session data is encoded in the URL fragment and is not sent to a session server by the app.</p><p>Your device may retain local payer-reported progress until you clear browser data. The app does not use analytics for UPI IDs, merchant names, bill amounts, transaction references or full session URLs.</p></article><Footer/></main>}
