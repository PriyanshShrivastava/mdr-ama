import { describe, expect, it } from "vitest";
import { buildUpiUri, createAttemptReference, parseMerchantUpiQr, validateMerchant } from "./upi";
describe("UPI",()=>{it("whitelists merchant QR fields",()=>{const result=parseMerchantUpiQr("upi://pay?pa=SHOP%40UPI&pn=Shop&mc=1234&am=2");expect(result.dynamic).toBe(true);expect(result.merchant).toMatchObject({pa:"shop@upi",pn:"Shop",mc:"1234"});expect(parseMerchantUpiQr("upi://pay?pa=shop%40upi&pn=Shop").dynamic).toBe(false);});it("rejects unsupported or invalid merchant data",()=>{expect(()=>parseMerchantUpiQr("https://example.com")).toThrow(/supported/);expect(()=>validateMerchant({pa:"bad",pn:"Shop",source:"manual"})).toThrow(/valid UPI/);expect(()=>validateMerchant({pa:"shop@upi",pn:"",source:"manual"})).toThrow(/merchant name/);});it("generates an encoded URI",()=>{const uri=buildUpiUri({pa:"shop@upi",pn:"A & B",source:"manual"},200000,0,1,"123");expect(uri).toContain("am=2000.00");expect(uri).toContain("pn=A+%26+B");expect(uri).toContain("tr=123");});it("creates numeric attempt references",()=>{expect(createAttemptReference()).toMatch(/^\d{14}$/);});});
